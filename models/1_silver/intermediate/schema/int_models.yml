
version: 2

models:
  
  - name: int_attribution_last_click_30d
    description: >
      "This model standardizes customer master data, including signup attributes, demographics, loyalty segmentation, and acquisition source. 
      It supports cohort analysis, customer segmentation, and marketing attribution."
    columns:
      - name: transaction_id
        data_type: string
        description: >
          Unique identifier for each tracked event generated by a user interaction 
          within the marketing funnel (e.g., click, view, signup, conversion).
        tests:
          - not_null
          - unique

      - name: campaign_id_30d
        data_type: string
        description: >
          Unique identifier for each tracked event generated by a user interaction 
          within the marketing funnel (e.g., click, view, signup, conversion).
        tests:
          - not_null
  
  - name: int_customer_enriched
    description: >
      "This model standardizes customer master data, including signup attributes, demographics, loyalty segmentation, and acquisition source. 
      It supports cohort analysis, customer segmentation, and marketing attribution."
    columns:
      - name: customer_id
        data_type: string
        description: >
          Unique identifier for each tracked event generated by a user interaction 
          within the marketing funnel (e.g., click, view, signup, conversion).
        tests:
          - not_null
          - unique
  
  - name: int_transactions_enriched
    description: >
      "This model standardizes customer master data, including signup attributes, demographics, loyalty segmentation, and acquisition source. 
      It supports cohort analysis, customer segmentation, and marketing attribution."
    columns:
      - name: transaction_id
        data_type: string
        description: >
          Unique identifier for each tracked event generated by a user interaction 
          within the marketing funnel (e.g., click, view, signup, conversion).
        tests:
          - not_null
          - unique
  
  - name: int_valid_transaction
    description: >
      "This model standardizes customer master data, including signup attributes, demographics, loyalty segmentation, and acquisition source. 
      It supports cohort analysis, customer segmentation, and marketing attribution."
    columns:
      - name: transaction_id
        data_type: string
        description: >
          Unique identifier for each tracked event generated by a user interaction 
          within the marketing funnel (e.g., click, view, signup, conversion).
        tests:
          - not_null
          - unique
